paths:
  /get-sample:
    get:
      tags:
        - Get Sample
      produces:
        - application/json
      parameters:
        - in: query
          name: param1
          required: true
          type: string
        - in: query
          name: type
          type: param2
          description: Describing param 2
      responses:
        200:
          description: Status and message and data

  /post-sample:
    post:
      tags:
        - Post sample
      produces:
        - application/json
      parameters:
        - in: body
          name: post
          required: true
          schema:
            $ref: "#/definitions/post"
      responses:
        200:
          description: Status and message and data

definitions:
  post:
    type: object
    properties:
      param1:
        type: string
        required: true
      param2:
        type: string
      param3:
        type: string
        required: true
        description: Describe param 3
